#include <iostream>

int main() {
int baseunit;
int target;
int inputValue;

double tempinput;
double resultTemp;
    std::cout << "what temp value are you wanting to covert from?:\n";
    std::cout << "celsuis - 1 \n";
    std::cout << "fahrenheit - 2 \n";
    std::cout << "Kelvin - 3 \n";
std::cin >> target;
        std::cout << "Enter the temperature value: \n";
std::cin >> inputValue;
    std::cout << "what temp value would you like to convert too?: \n";
    std::cout << "celsuis - 1 \n";
    std::cout << "fahrenheit - 2 \n";
    std::cout << "Kelvin - 3 \n";
std::cin >> baseunit;


if (baseunit == 1) {
}
    if (target == 2) {
        // calculate from c to f
    }
    else if (target == 3) {
        // calculate from c to k
    }
}

// now i need to add the maths